'use strict';

const { Modules } = require('../models/Modules');

/** @type {import('sequelize-cli').Migration} */
module.exports = {
  async up (queryInterface, Sequelize) {    
      //INSERTED BY ROUTINES SEEDER
  },

  async down (queryInterface, Sequelize) {
     //HANDLED BY ROUTINES SEEDER
  }
};
